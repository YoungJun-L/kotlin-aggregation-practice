package org.example.aggregate.quiz2

val words = listOf("TONY", "a", "hULK", "B", "america", "X", "nebula", "Korea")

/**
 * 단어의 첫 글자 기준 리스트 내 빈도 수
 * @return {T=1, a=2, h=1, B=1, X=1, ...}
 */
fun quiz1(): Map<Char, Int> {
    return emptyMap()
}

/**
 * 단어의 길이가 2 이상만 첫 글자를 대문자로 변환 후 공백으로 구분하여 합친 하나의 문자열
 *
 * *e.g. `[`hello, Abc, Island, b`]` -> H I*
 * @return T H A N K
 */
fun quiz2(): String {
    return ""
}
